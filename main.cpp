#include <bits/stdc++.h>

#define ll long long
using namespace std;

int main() {
#ifdef LOCAL
  auto in = ifstream("input.txt");
  cin.rdbuf(in.rdbuf());
#else
  cin.tie(0)->sync_with_stdio(0);
#endif

  int n = 0;
  cin >> n;

  cout << n;
}