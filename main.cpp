#include <bits/stdc++.h>
#include <fstream>

#define ll long long
using namespace std;

int main() {
#ifdef LOCAL
  auto in = ifstream("input.txt");
  cin.rdbuf(in.rdbuf());
#endif

  int n = 0;
  cin >> n;
}